# 🧠 Gemini & Antigravity Persistent Memory
### *MultiPDF Doc (multipdfdoc.com): 100% In-Browser Private Document Suite*

> [!IMPORTANT]
> **Active Memory & Rules Checkpoint**
> This file is automatically loaded at the start of every session.
> 
> 1. **Core Architecture:** Client-side WebAssembly execution via `pdf-lib`. No files uploaded to servers.
> 2. **Design Standard:** Light Theme Soft Claymorphism strictly governed by [design.prd](file:///d:/adsense/design.prd).
> 3. **Header Rule:** Floating wide capsule (`max-w-[1520px]`, `h-14 sm:h-16`), single-line text (`whitespace-nowrap`).
> 4. **AdSense E-E-A-T:** Every tool has a 700+ word `<ValueWrapper />` with automated `FAQPage` schema.
> 5. **Dev/Build:** `npm run dev` / `npm run build`. Verified 26/26 routes pre-rendered with code 0.
> 6. **Oracle Cloud Backend:** 12GB RAM instance with PaddleOCR v4 and TrOCR (`oracle-backend/`).
> 7. **Full System Context:** Read [brain.md](file:///d:/adsense/brain.md), [propermonth.md](file:///d:/adsense/propermonth.md), [AGENTS.md](file:///d:/adsense/AGENTS.md), and [design.prd](file:///d:/adsense/design.prd).
